version: 2.1

orbs:
  cli: circleci/circleci-cli@0.1.9

jobs:
  validation:
    executor: cli/default
    steps:
    - cli/install
    - checkout
    - run:
        name: "バリデーション"
        command: circleci orb validate ./src/hello-orb.yml

workflows:
  main:
    jobs:
      - validation